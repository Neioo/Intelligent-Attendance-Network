streamlit
streamlit-webrtc
# CRITICAL: Force NumPy 1.x to prevent Segfaults
numpy<2.0.0
# CRITICAL: Use headless only. Do not list 'opencv-python' anywhere.
opencv-python-headless
ultralytics
deepface
pandas
# Use a flexible version for av to ensure it builds
av
