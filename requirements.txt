streamlit
streamlit-webrtc
# CRITICAL: NumPy 2.x causes the Segmentation Fault. We must force v1.
numpy<2.0.0
# CRITICAL: Use headless only.
opencv-python-headless
ultralytics
deepface
pandas
av
# Optional: explicitly pin tf-keras to avoid mismatches if DeepFace acts up
tf-keras
